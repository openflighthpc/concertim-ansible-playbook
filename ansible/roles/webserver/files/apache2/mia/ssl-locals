# SSLCertificateFile /etc/ssl/certs/apache_crt.pem
# SSLCertificateKeyFile /etc/ssl/private/apache_key.pem
# SSLCertificateChainFile /etc/ssl/certs/cacerts.pem

# FSR the above definitions don't work whilst the following do.  We need to
# decide what the SSL certs story is going forwards.
#
# Concurrent/Concertim used self-signed certificates and required their users
# to add the signing authority to their browser.
#
# Alternatively, we could use Let's Encrypt and deal with certificate updating
# somehow.
#
SSLCertificateFile    /etc/ssl/certs/ssl-cert-snakeoil.pem
SSLCertificateKeyFile /etc/ssl/private/ssl-cert-snakeoil.key
